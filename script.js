function comparison(firstStr,SecStr) {
    if (firstStr.length === SecStr.length){
        console.log(true);
    }
    else{
        console.log(false);
    }
}
console.log(comparison("saba", "tako"));
